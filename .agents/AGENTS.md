# Workspace Customization Rules

- Whenever any changes are made to the codebase, they must be committed and pushed to the GitHub repository (`my-portfolio` and `origin`) to trigger Vercel deployment updates automatically.
